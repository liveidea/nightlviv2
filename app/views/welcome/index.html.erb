<body class="body1">
 <div class="container" style="margin-top: 60px">
	<div class="row">
	  <% @clubs.each do |c| %>
	  	<div class="block2">
	  	<h2><center><%= link_to c.name, images_path(c) %></center></h2>	
	    <center><%-c.photos.random.limit(5).each do |cc| %>
	     <%= image_tag cc.image_url(:thumbnail1), {:class => 'img_rounded'} %>
	     <% end %></center>
		</div>
	  <% end %>
	</div>
 </div>	  
</body>